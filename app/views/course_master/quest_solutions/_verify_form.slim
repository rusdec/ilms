= button_to text, path, { action: :patch,
                          remote: true,
                          format: :json,
                          method: :patch,
                          class: "btn #{btn_class} mr-1",
                          form: { class: "#{text.underscore}_quest_solution" } }
